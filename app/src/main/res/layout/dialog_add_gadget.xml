<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- EditText for Gadget Name -->
    <EditText
        android:id="@+id/edit_text_gadget_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Gadget Name"
        android:textSize="22sp" />

    <!-- EditText for Gadget Description -->
    <EditText
        android:id="@+id/edit_text_gadget_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Gadget Description"
        android:textSize="22sp"
        android:layout_marginTop="10dp" />

    <!-- Horizontal LinearLayout for Save and Cancel buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="20dp">

        <!-- Save Button -->
        <Button
            android:id="@+id/button_save"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Save" />

        <!-- Space between the buttons -->
        <Space
            android:layout_width="16dp"
            android:layout_height="wrap_content" />

        <!-- Cancel Button -->
        <Button
            android:id="@+id/button_cancel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Cancel" />
    </LinearLayout>
</LinearLayout>
